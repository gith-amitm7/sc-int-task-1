/**
 * Only selected actions must be accessible by components
 * Hence operations are actions which would be used directly by
 * desired components
 */
import { login } from "./reducer";

export { login };
